# Configuration for cargo-deny
# https://github.com/EmbarkStudios/cargo-deny

[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "warn"
# Lint level for when a crate marked as "deny" is detected
deny = "warn"
# List of explicitly disallowed crates
skip = []
# Skip certain crates when checking for duplicates
skip-tree = []

[licenses]
# List of explicitly allowed licenses
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-DFS-2016",
    "CC0-1.0",
    "MPL-2.0",
    "Unlicense",
]
# List of explicitly disallowed licenses
deny = []
# Lint level for when a crate doesn't have a license
unlicensed = "deny"
# Lint level for licenses not explicitly allowed or denied
default = "deny"
# Confidence threshold for license detection (0.0 to 1.0)
confidence-threshold = 0.8
# Allow licenses in private crates
private = { ignore = false }

[sources]
# Lint level for what to happen when a crate from a crate registry that is not in the allow list is detected
unknown-registry = "warn"
# Lint level for what to happen when a crate from a git repository that is not in the allow list is detected
unknown-git = "warn"
# List of allowed crate registries
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# List of allowed Git repositories
allow-git = []

[advisories]
# The path where the advisory database is cloned/fetched into
db-path = "~/.cargo/advisory-db"
# The url(s) of the advisory databases to use
db-urls = ["https://github.com/rustsec/advisory-db"]
# The lint level for security vulnerabilities
vulnerability = "deny"
# The lint level for unmaintained crates
unmaintained = "warn"
# The lint level for crates with security notices
notice = "warn"
# A list of advisory IDs to ignore
ignore = []
# The lint level for yanked crates
yanked = "warn"